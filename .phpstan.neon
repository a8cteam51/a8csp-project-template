includes:
    - vendor/a8csp/configs/php/quality-assurance/phpstan.dist.neon

parameters:
    # One run covers both tracked components: a site has a single WordPress floor.
    paths:
        - %currentWorkingDirectory%/themes/a8csp-project-template
        - %currentWorkingDirectory%/mu-plugins
    WPCompat:
        # wp-compat's fallback finds no entry file at a site root, so the one carrying the floor is named.
        pluginFile: %currentWorkingDirectory%/mu-plugins/a8csp-project-template-features/a8csp-project-template-features.php
