name: Audit

on:
  pull_request:
  push:
    branches: [trunk, develop]
  schedule:
    # Catches newly disclosed advisories between dependency bumps.
    - cron: '0 7 * * 1'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  supply-chain:
    name: Supply-chain audit
    uses: a8cteam51/a8csp-configs/.github/workflows/reusable-supply-chain-audit.yml@67392ce62cd343bc5ce8c65da264c778cdd86210 # v1.0.0
